{
  "name": "monte",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "sync:codegen": "pnpm --filter @monte/backend exec convex codegen --typecheck disable && node scripts/sync-convex-codegen.mjs",
    "build:shared": "pnpm sync:codegen && pnpm --filter @monte/types build && pnpm --filter @monte/api build",
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "check:codegen": "node scripts/check-codegen.mjs"
  }
}
