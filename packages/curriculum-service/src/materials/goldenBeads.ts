import manifest from '../../assets/manipulatives/golden-beads/manifest.json';

export type GoldenBeadManifest = typeof manifest;

export const goldenBeadManifest = manifest;

export const goldenBeadNodes = manifest.nodes;
