import type { Component } from 'solid-js';

const Home: Component = () => {
  return (
    <section class="space-y-4">
      <h1 class="text-2xl font-semibold text-neutral-100">Workspace Overview</h1>
      <p class="text-sm text-neutral-400">
        Frontend and backend packages are ready. Build product routes, forms, and data tables from here.
      </p>
    </section>
  );
};

export default Home;
